<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-dark fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand js-scroll-trigger" href="#page-top">Jepun Putih</a>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      Pilih Destinasi
      <i class="fa fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav text-uppercase ml-auto">
        <% for(let j = 0 ; j < destination.length; j++){ %>
          <% for(let i = 0 ; i < destination[j].province.length; i++ ){ %>

        <li class="nav-item">
          <a class="nav-link js-scroll-trigger active" href="/TravelTo/<%= destination[j].province[i].province_name %>"><%= destination[j].province[i].province_name %></a>
        </li>
          <% } %>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
